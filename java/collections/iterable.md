# Iterable

Root interface for collections which allows iterating them.

## Traversing

It can be iterated:

```java
col.iterator();
```

And it can be used in for-each loops:

```java
for(String s : col)
```



